model:
  architecture: "simple_cnn"
  num_classes: 10

data:
  data_dir: "./data"

training:
  batch_size: 64
  learning_rate: 0.001
  epochs: 20
  early_stopping_patience: 5

output:
  checkpoint_dir: "./checkpoints"
  model_name: "best_model.pt"
